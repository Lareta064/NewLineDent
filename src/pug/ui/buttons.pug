include ../layouts/_mixins-links.pug
block variables

<!DOCTYPE html>
html(lang="ru")
	head
		meta(charset="UTF-8")
		meta(name="viewport", content="width=device-width, initial-scale=1.0")
		meta(http-equiv="X-UA-Compatible", content="ie=edge")

		//====убрать синий цвет у ссылок в сафари ========
		meta(name="format-detection" content="telephone=no")
		link(rel="stylesheet", href="../css/main.css")
		title UI-KIT

	body.body-dashboard

		include ui-navigation.pug

		block content
			.container-dashboard(style="background:#e0e0e0;")
				h2 Кнопки

				.container-dashboard__content
					button.btn Заказать
					pre.
						button.btn Заказать
					hr
					a(href="#!").btn Заказать
					pre.
						a(href="#!").btn Заказать
					hr
					input.btn(type="submit" value="Заказать")
					pre.
						input.btn(type="submit" value="Заказать")
					hr
					button.btn-primary Заказать
					pre.
						button.btn-primary  Заказать
					hr
					button.btn-outline Дополнительно
					pre.
						button.btn-outline Дополнительно
					